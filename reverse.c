#include<stdio.h>
#include<string.h>
int main()
{
	int i,l,n,j;
	j=0;
	char st[100];
	printf("Enter the string to be reversed:",st);
	scanf("%c",st);
	l=printf("enter the length of the string:");
	scanf("%d",&l);
    return 0;		
}
		
//   for(i=l;i>=0; i--)
  // { 
    //    printf("%c", st[i]);
   //}

//return 0;
//}

